fn main() {
    let p = path_join("foo", "bar")
    print(p)
    let t0 = now_ms()
    sleep_ms(1)
    let t1 = now_ms()
    if t1 - t0 >= 0 {
        print_int(1)
    } else {
        print_int(0)
    }
}
