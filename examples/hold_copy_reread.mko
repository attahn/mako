// Copy fields on hold structs may be read repeatedly
struct Point {
    x: int
    y: int
}

fn main() {
    hold let p = Point { x: 1, y: 2 }
    print_int(p.x)
    print_int(p.x)
    print_int(p.y)
}
