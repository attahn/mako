fn TestArenaMake() {
    arena a {
        let mut s = make([]int, 2, 4)
        s[0] = 5
        s[1] = 6
        assert_eq(len(s), 2)
        assert_eq(s[0], 5)
        let mut b = make([]byte, 1)
        b[0] = byte(7)
        assert_eq(int(b[0]), 7)
    }
}
