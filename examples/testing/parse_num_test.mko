fn TestParseNum() {
    match parse_int("42") {
        Ok(v) => assert_eq(v, 42),
        Err(_) => assert(false),
    }
    match parse_int("x") {
        Ok(_) => assert(false),
        Err(_) => assert(true),
    }
    assert(parse_float("2.5") > 2.0)
}
