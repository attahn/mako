struct Point {
    x: int
    y: int
}

fn TestArenaStructSlice() {
    arena a {
        let mut xs = make([]Point, 2, 4)
        xs[0] = Point { x: 1, y: 2 }
        xs[1] = Point { x: 3, y: 4 }
        assert_eq(len(xs), 2)
        assert_eq(xs[0].x, 1)
        assert_eq(xs[1].y, 4)
    }
}
