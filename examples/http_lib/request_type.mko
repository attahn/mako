// Typed HttpRequest — parse raw HTTP/1.1 (same shape used under TLS after decrypt).
fn main() {
    let raw = "GET /hello HTTP/1.1\r\nHost: localhost\r\n\r\n"
    let req = http_request_parse(raw)
    print(http_request_method(req))
    print(http_request_path(req))
}
