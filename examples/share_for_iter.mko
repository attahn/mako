// Share in for-range body ends each iteration — re-borrow next iter OK.
fn main() {
    let mut x = 1
    for i in range 2 {
        share let s = share_int(x)
        print_int(share_get(s))
        print_int(i)
    }
    x = 2
    print_int(x)
}
